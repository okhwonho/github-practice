Terminal 에서 입력

git status : 변경 내용이 있는 지 확인
git add -A : unstaged(변경사항이 있는) 상태의 파일을 staged 상태로 변경
git commit -m "변경내용에 대한 간략한 description" : 수정사항 Git 에 반영

git branch : 현재 branch 확인
git branch aaa : aaa 라는 branch 추가
git checkout aaa : aaa 라는 branch 로 이동
git checkout -b aaa : aaa라는 branch를 추가하면서 바로 branch로 이동하여 작업

git log : commit 이력 확인
git log --graph --all --decorate : commit 이력, branch 등을 그래픽으로 표현

git reset 111111 : git log로 확인할 수 있는 commit 넘버 앞 6자리를 이용하여  (--hard 일 경우 111111 이후 시점 Commit은 제거)
git revert 222222 : 222222 commit 에 대한 commit 되돌리기를 하여 변경 이전 상태로 이동


git remote : 현재 연결되어 있는 github 를 표현
git push : 로컬에 있는 commit 을 github로 전송

